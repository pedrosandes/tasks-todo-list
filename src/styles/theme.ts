import { DefaultTheme } from "styled-components"


/***********
* Theme da aplicação, aqui fica as cores, tamanho de fontes, font-famlliy ou
* qualquer outra coisa que queira utilizar como estilo padrão do projeto.
*/
const theme: DefaultTheme = {
  colors: {
    black: "#121212",
    white: "#F5F5F5",
    blue: "#2962FF"
  }
}

export default theme
